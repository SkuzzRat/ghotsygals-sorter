import LayerExtractor from './LayerExtractor';

export const metadata = {
  title: '👻 Ghosty Gals Layer Extractor',
  description: 'Upload your PSD, name your layers, export as PNGs~',
};

export default function LayerExtractorPage() {
  return <LayerExtractor />;
}
